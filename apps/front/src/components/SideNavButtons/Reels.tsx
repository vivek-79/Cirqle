import SideNavButton from "./Button"
import { TfiVideoClapper } from "react-icons/tfi";

const Reels = () => {
    return (
        <div>
            <SideNavButton Icon={TfiVideoClapper} size={23} content="Reels"/>
        </div>
    )
}

export default Reels;